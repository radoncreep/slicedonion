import React from 'react';
import { StyleSheet, View } from 'react-native';

export const HeaderNavigation = ({ headerName = "Home " }) => {
    return (
        <View style={styles.container}>
             
        </View>
    )
};

const styles = StyleSheet.create({
    container: {

    }
})