import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const GenreListComponent = () => {

    return (
        <View style={styles.container}>
            {/* genre component will be here */}
            <Text>Goes here</Text>
        </View>
    )
};

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fff'
    },
});